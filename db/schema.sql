DROP DATABASE IF EXISTS critter_db;

CREATE DATABASE critter_db;

/* USE critter_db;

CREATE TABLE critter (
    id INT NOT NULL,
    critter_name VARCHAR(30) NOT NULL,
    animal_description TEXT NOT NULL,
    date_uploaded DATETIME DEFAULT CURRENT_TIMESTAMP NOT NULL
); */